# Wait With Me — privacy policy

The published policy for the Wait With Me app (`com.utrendtek.waitwithme`),
served by GitHub Pages at https://utrendtek.github.io/wait-with-me-privacy/

The source of truth is `legal/site/index.html` in the `wait-with-me` repo.
Edit it there, then copy it here — never the other way round.
